# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'StartWarsRemake' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
     pod 'Alamofire', '~> 5.0.0-rc.2'

  # Pods for StartWarsRemake

  target 'StartWarsRemakeTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Alamofire', '~> 5.0.0-rc.2'
  end

  target 'StartWarsRemakeUITests' do
    # Pods for testing
    pod 'Alamofire', '~> 5.0.0-rc.2'
  end

end
